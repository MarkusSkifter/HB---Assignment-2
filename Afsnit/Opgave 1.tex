\section{Question 1: Weighted Household Utility}\label{sec:q1}

\subsection*{Model Change}

In the baseline model from, the household utility function is an equal-weighted sum of individuals utilities:
%
\begin{equation}\label{eq:baseline_util}
    U(c_t, h_{1,t}, h_{2,t}) = 2\frac{(c_t/2)^{1+\eta}}{1+\eta} - \rho_1 \frac{h_{1,t}^{1+\gamma}}{1+\gamma} - \rho_2 \frac{h_{2,t}^{1+\gamma}}{1+\gamma}
\end{equation}
%
We then introduce the weight $\mu \in [0,1]$ on member 1 and $(1-\mu)$ on member 2. 
\newline
The individual utilities are:
%
\begin{equation}
    u_j(c_t, h_{j,t}) = \frac{(c_t/2)^{1+\eta}}{1+\eta} - \rho_j \frac{h_{j,t}^{1+\gamma}}{1+\gamma}, \quad j \in \{1,2\}
\end{equation}
%
The new weighted household utility is the convex combination:
%
\begin{equation}\label{eq:weighted_util}
    U(c_t, h_{1,t}, h_{2,t}) = \mu \, u_1(c_t, h_{1,t}) + (1-\mu) \, u_2(c_t, h_{2,t})
\end{equation}
%
Since $\mu + (1-\mu) = 1$, expanding gives:
%
\begin{equation}
    U = \frac{(c_t/2)^{1+\eta}}{1+\eta} - \mu \, \rho_1 \frac{h_{1,t}^{1+\gamma}}{1+\gamma} - (1-\mu) \, \rho_2 \frac{h_{2,t}^{1+\gamma}}{1+\gamma}
\end{equation}
%
The only change relative to the baseline is that the labor-disutility weights are now $\mu$ and $(1-\mu)$ rather than $1$ and $1$.

\subsection*{Implementation}

We add a single parameter \texttt{par.mu = 0.5} to the model class. In the \texttt{util()} function, the disutility weights become \texttt{par.mu} for member 1 and \texttt{1-par.mu} for member 2, and the consumption term is \texttt{(cons/2)**(1+eta)/(1+eta)} with no leading factor of 2.

\medskip
\noindent\textbf{Note on the comparison:} We benchmark the weighted utility at $\mu = 0.5$ against the \emph{genuine} baseline in equation~\eqref{eq:baseline_util} --- the original equally-weighted utility with the factor of $2$ on consumption and unit weights on labour. As shown in Question~2, the $\mu = 0.5$ objective equals exactly $\tfrac{1}{2}$ of this baseline. This is a \emph{cardinal} rescaling, but utility is \emph{ordinal}: the argmax is invariant to any positive scaling, so the two specifications should produce the same labour supply. We verify this numerically by solving both models and comparing the simulated hours.
\medskip

The check confirms it --- the two objectives differ by a factor of $2$, yet the optimal hours coincide down to the optimizer's numerical tolerance:

\begin{align*}
    \max|\Delta h_1| &= 9.76\times 10^{-5} \\
    \max|\Delta h_2| &= 1.03\times 10^{-4}
\end{align*}

These residuals are not zero ``by construction'' (the baseline here is a structurally different, factor-of-$2$ utility); they are at the level of the solver's convergence tolerance, confirming that the cardinal rescaling leaves behaviour unchanged.

% ---------------------------------------------------------------
% PLACEHOLDER PLOT
% ---------------------------------------------------------------

\begin{figure}[H]
    \centering
    \includegraphics[width=1\linewidth]{Billeder/Baseline_vs_mu05_hours.png}
    \caption{Average hours per period under the baseline and $\mu = 0.5$; lines overlap, confirming identical behavior.}
    \label{fig:placeholder}
\end{figure}

